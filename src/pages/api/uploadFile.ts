/**
 * We will hit upload file api in order to upload our files to cloudinary.
 */
import { v2 } from "cloudinary";
import { NextRequest, NextResponse } from "next/server";

export default function uploadFileHandler(req: NextRequest, res: NextResponse) {
	try {
	} catch (error) {}
}
